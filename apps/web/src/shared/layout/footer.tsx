export function Footer() {
  return (
    <footer className="relative border-t border-glass-border">
      <div className="mx-auto flex w-full flex-col gap-2 px-4 py-6 sm:flex-row sm:items-center sm:justify-between sm:px-6">
        <p className="text-xs text-ink/60">AIWorld</p>
        <p className="text-xs text-ink/40">
          A dark glassmorphism exploration of worlds and characters.
        </p>
      </div>
    </footer>
  );
}
