export function Footer() {
  return (
    <footer className="relative z-10 border-t border-glass-border">
      <div className="mx-auto flex w-full max-w-7xl flex-col gap-2 px-4 py-6 sm:flex-row sm:items-center sm:justify-between sm:px-6 lg:px-8">
        <p className="text-xs text-ink/60">AIWorld</p>
        <p className="text-xs text-ink/40">
          A dark glassmorphism exploration of worlds and characters.
        </p>
      </div>
    </footer>
  );
}
